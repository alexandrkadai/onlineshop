1.Text for pages about in two languages 
2. Prototype of clothes
3. Prices 

Steps to complete store : 
1. database and fetching products.
2. show products and images in product description and main page, provide data types.
3. Redux -> cart delete action, cart tottal amount , total sum. 
4. Checkout page -> create new form, novaposhta searching fields, send order to database.
5. Payment liquipay integration, testing payments. 
6. i18next  translating all stuff. 
7. Depoly and test in hostinger. Apply real transactions on hiden object, create secret route.

  {cities.map((item: any) => (
              <option key={item.Description}>{item.Description}</option>
            ))}

             <option>Виберіть Відділення</option>
            {some ? (
              cities.map((item: any) => <option key={item.SiteKey}>{item.Description}</option>)
            ) : (
              <option>loading</option>
            )}


1. Contry region Oblast in the Delivery 
2. Arrow after form complete 
3. Languages. 
4. Different Colors. 
5. Position fixed order summary.
6. Payments System